import { Component } from "react"

const num1, num2, resul = ' '

export default class OperatorLogic extends Component {
    
    if operator === '-',
        num1 - num2
    render () {
        
    }
}